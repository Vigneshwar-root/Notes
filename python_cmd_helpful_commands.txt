echo > requirements.txt

//pip install if it does not reflect in virtual environment
python -m pip install selenium
py -3 -m pip install selenium

// Install all the pip installations in one shot

pip install -r requirements.txt
py -3 -m pip install -r requirements.txt

//requirements text file example 

requests==2.26.0
Flask==2.1.0
numpy==1.21.5



Creating virtual environment in python

py -3 -m venv myenv

myenv\Scripts\activate - in windows
myenv\Scripts\activate.bat - activate
deactivate - to close the session of virtual environment



colorgram.py - python library that lets you extract colors from images 
  import colorgram

  # To extract 6 colors from an image 
  colors = colorgram.extract('sweet_pic.jpg',6)
